# Node
frontend/node_modules/
frontend/dist/

# Python
__pycache__/
*.pyc
backend/.pytest_cache/
backend/benchmark_report.json

# Model artifacts / big binaries (mount or download at runtime, don't commit)
G2PWModel/
model_quant.onnx
am.mvn
*.wav
# NOTE: asr_example.wav is the ground-truth fixture for the e2e STT accuracy test
# (backend/test_e2e_report.py) — it must be committed, the *.wav rule above was
# excluding it, so a fresh clone could not run its own accuracy suite.
!asr_example.wav
/tmp/kokoro/
/tmp/qwen3_tts/
/tmp/XASR/
/tmp/llms/

# Local scratch / agent tooling dirs (never part of the demo)
.claude/
workspace/

# Secrets — a token file must never be committed
.env
*.token

# OS / editor
.DS_Store
*.log
.idea/
.vscode/
